# 测试环境配置 - 连接统一测试数据库
spring:
  application:
    name: system-server
  cloud:
    nacos:
      discovery:
        namespace: test
        server-addr: mse-553b8e52-nacos-ans.mse.aliyuncs.com
        # 如果 MSE Nacos 开启了鉴权，需要配置 accessKey 和 secretKey（或者用户名密码，具体看 MSE 的鉴权方式）
        # 或者使用用户名密码方式（根据 MSE 的配置选择一种）
        group: DEFAULT_GROUP  # 服务分组，默认即可，也可以自定义
        # 其他配置
        heart-beat-interval: 5000
        heart-beat-timeout: 15000
        register-enabled: true
      config:
        namespace: test
        server-addr: mse-553b8e52-nacos-ans.mse.aliyuncs.com
        group: DEFAULT_GROUP  # 配置分组，默认即可
        file-extension: yaml  # 配置文件后缀，默认为 properties，推荐使用 yaml
        # 共享配置（如果有）
        # shared-configs[0]:
        #   data-id: common.yaml
        #   group: DEFAULT_GROUP
        #   refresh: true
        # 扩展配置
        # extension-configs[0]:
        #   data-id: ext.yaml
        #   group: DEFAULT_GROUP
        #   refresh: true
  datasource:
    url: jdbc:mysql://bajiezu-ark-test.rwlb.rds.aliyuncs.com:3306/bajiezu_ark_test?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: 'bajiezu_ark_test'
    password: '*BhZ!6jC(n$43Zf)qa1!wt8aq'
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 30
      minimum-idle: 10
  data:
    redis:
      host: bajiezu-ark-test.redis.rds.aliyuncs.com
      port: 6379
      password: 'N5GiF6ex@YMKdj0cfV(jEc('
      database: 0
      username: bajiezu_ark_test


logging:
  level:
    com.baomidou: DEBUG
    org.mybatis: DEBUG
    org.springframework.jdbc: DEBUG
    com.bajiezu: DEBUG


